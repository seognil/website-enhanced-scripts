## 显示 bilibili 视频 点赞率

把 B 站已加载的数据（浏览量、点赞数 等）计算成百分比，显示到界面上

目前支持的页面：

- 列表卡片：首页、分区页、搜索页、稍后再看
- 视频播放：普通视频页、Bangumi（动画、电影）

（并非所有页面都能显示，有些页面没有相关数据，比如 up 空间中的视频列表，目前没写主动获取点赞量的逻辑，因此不显示）

- 按照数据高低，显示不同颜色（白绿蓝紫橙）

（目前是手动统计分布，仅普通视频的统计较为合理，番剧的样本还不充足，后续再迭代）
